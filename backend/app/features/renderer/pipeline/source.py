"""L0 — Source layer: validate + preprocess source video.

Source validation logic is embedded in render_pipeline.py (stage 0).
Local video files only — platform downloads are handled by features/downloader/.
"""

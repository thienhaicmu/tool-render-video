"""GĐ2 Visual Foundation — parts-based anime character + layered scene factories.

NOT wired into the render pipeline yet (GĐ2 DoD: the contact sheets must be
approved by eye first — see backend/scripts/build_visual_v2_contact_sheets.py).
Everything here is pure str→str SVG composition: no I/O, no DB, never raises.
"""

# Optional local AI dependencies — install only when AI features are needed.
# pip install -r backend/requirements-ai.txt
sentence-transformers
faiss-cpu
librosa
mediapipe
faster-whisper
whisperx
# Cloud AI providers (optional — falls back to openai SDK if not installed)
groq>=0.4.0
openai>=1.0.0

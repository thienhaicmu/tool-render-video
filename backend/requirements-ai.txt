# Optional local AI dependencies — install only when AI features are needed.
# pip install -r backend/requirements-ai.txt
sentence-transformers
faiss-cpu
librosa
mediapipe
faster-whisper
whisperx
# Cloud LLM providers for segment selection (Phase I — multi-provider).
# Each is optional; install the one matching your configured ai_provider.
groq>=0.4.0                # default provider
openai>=1.0.0              # ai_provider=openai (also used by Groq fallback path)
google-genai>=2.0.0        # ai_provider=gemini (free 1M tokens/day)
anthropic>=0.40.0          # ai_provider=claude (paid)

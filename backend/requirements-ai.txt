# Optional local AI dependencies — install only when AI features are needed.
# pip install -r backend/requirements-ai.txt
sentence-transformers
faiss-cpu
librosa
mediapipe
# faster-whisper: 4x faster than openai-whisper on the same model+device.
faster-whisper
whisperx
# CUDA 12 runtime DLLs bundled by pip (cublas64_12.dll, cudnn*.dll, nvrtc).
# Required for faster-whisper / ctranslate2 GPU acceleration on Windows when
# the system has only the NVIDIA driver (no CUDA Toolkit). Without these,
# ctranslate2 fails with "Library cublas64_12.dll is not found" and the
# adapter falls back to openai-whisper CPU (~5-10x slower).
nvidia-cublas-cu12
nvidia-cudnn-cu12
nvidia-cuda-nvrtc-cu12
# Cloud LLM providers for segment selection.
# Each is optional; install the one matching your configured ai_provider.
openai>=1.0.0              # ai_provider=openai
google-genai>=2.0.0        # ai_provider=gemini (default — free 1M tokens/day)
anthropic>=0.40.0          # ai_provider=claude

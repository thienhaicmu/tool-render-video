# Sprint 1 Closure — Clean, YouTube Removal, Doc Sync

**Date opened:** 2026-06-04
**Date closed:** 2026-06-04 (partial — 1.4 Temp File Audit deferred)
**Branch:** `feature/render-engine-upgrade`
**Plan reference:** `docs/review/SPRINT_PLAN_2026-06-04.md`

## Scope completed

| Sub | Commit | Summary |
|---|---|---|
| Pre-flight | `4fb10df` | Fixed `prompts.py:80-81` literal `{end}`/`{start}` brace escape — restored Group A baseline (3 LLM provider tests). Tagged `pre-sprint1-baseline`. Backed up `data/app.db`. Wrote sprint plan doc. |
| 1.1 Dead code | `fc8b713` | Removed 5 confirmed-dead modules (`caption_engine.py`, `routes/platform_downloader.py` shim, `ai/analysis/groq/`, `frontend/src/api/download.ts`, `ai-team-framework` broken submodule). Archived mockup screenshots + `design/ui-prototype.html`. Audit correction: `services/platform_downloader/` is still active (imported by `features/downloader/router.py:27-28`) — kept. |
| 1.2 YouTube khỏi render path | `899c708` | Removed YouTube branch from `routes/render.py` (`prepare_source`, `quick_process`). Deleted `/api/render/download-health` endpoint + `DownloadHealthRequest`. Added `extra="ignore"` to `PrepareSourceRequest` and `QuickProcessRequest` so legacy payloads carrying `youtube_url`/`url` deserialize cleanly. Tightened frontend `api/render.ts` type to `source_mode: 'local'` only. Removed 4 dead i18n keys (EN+VI). NEW `tests/test_youtube_blocked_from_render.py` gate suite (5 tests). Standalone Downloader feature (`features/downloader/`, `services/downloader.py`, `services/cookie_extractor.py`) untouched and operational. |
| 1.5 Group B test reconcile | `561b72d` | Replaced 8 obsolete `TestEnsureH264Preview` tests (asserted old probe-and-transcode behaviour) with 2 contract tests pinning the new no-op behaviour (`_ensure_h264_preview` always returns `src` since commit `92233c5` "preview skip"). |
| 1.3 Doc sync | (this commit) | Fixed `PROJECT_MAP.md` (retired AI Director rows, corrected `routes/download.py` to `features/downloader/router.py`). Fixed `docs/ARCHITECTURE.md` (source-prep flow, removed Groq Phase 2 reference, corrected `/api/download` route prefix, replaced "AI Director" rows with LLM provider table). Fixed `docs/RENDER_PIPELINE.md` (source paths now two: editor session + local; YouTube source path row removed; `yt-dlp fallback` removed from fallback list). |

## Scope deferred

- **1.4 Temp File Audit (read-only):** 23 backend files have tempfile/tmp patterns. Inventory + classification will feed Sprint 6 (Temp File Optimization). Estimated 3-4 days of read-only work. Deferred to a separate sub-sprint when scheduled.

## Pytest baseline trajectory

| Stage | passed | skipped | failed |
|---|---:|---:|---:|
| Audit ledger baseline (Sprint 6.D close, 2026-06-02) | 2077 | 1 | 0 |
| Sprint 1 Pre-flight initial run | 2069 | 1 | 9 |
| After Group A fix (`4fb10df`) | 2072 | 1 | 6 |
| After 1.1 (`fc8b713`) | 2072 | 1 | 6 |
| After 1.2 (`899c708`) | 2077 | 1 | 6 |
| After 1.5 (`561b72d`) | **2077** | **1** | **0** |

The current baseline (`2077 / 1 / 0`) matches the Sprint 6.D ledger entry —
Group A and Group B drifts that arose between 2026-06-02 and 2026-06-04 are
both reconciled.

## Sacred contracts preserved

- Frozen API routes (`/api/render/process`, `/api/jobs/{id}`, WebSocket `/api/jobs/{id}/ws`) untouched.
- Sacred Contract #1 (result_json aliases): not in scope this sprint.
- Sacred Contract #2 (RenderRequest field defaults): no new required field. `extra="ignore"` covers backward compat for stored payloads carrying deprecated fields.
- Sacred Contract #3 (AI modules return None on failure): Pre-flight `prompts.py` fix restored the contract for `_USER_TEMPLATE.format()` — every provider now reliably calls the formatter without `KeyError`, so the try/except return-None layer above is no longer short-circuited.
- Sacred Contract #4-5 (job/part stage names): untouched.
- Sacred Contract #6 (`_emit_render_event` signature): untouched. `prepare_source` event payload now omits `youtube_url` from error contexts but the signature is unchanged.
- Sacred Contract #7 (`data/app.db`): no schema changes. Backed up before sprint start.
- Sacred Contract #8 (`qa_pipeline.py`): untouched.

## Files removed

```
backend/app/services/caption_engine.py           (-354 LOC)
backend/app/routes/platform_downloader.py        (-3 LOC shim)
backend/app/services/platform_downloader/        (kept — still imported)
backend/app/ai/analysis/groq/                    (folder + pycache)
frontend/src/api/download.ts                     (-41 LOC)
ai-team-framework                                (broken submodule gitlink)
backend/app/routes/render.py prepare_source()    (-47 LOC YouTube branch)
backend/app/routes/render.py download_health()   (-6 LOC entire route)
backend/app/routes/render.py quick_process()     (-44 LOC YouTube branch)
backend/app/models/schemas.py DownloadHealthRequest  (-3 LOC class)
backend/app/models/schemas.py PrepareSourceRequest.youtube_url  (-1 LOC field)
backend/tests/test_preview_ffmpeg_probers.py     (-126 / +23 LOC test rewrite)
frontend/src/i18n/translations.ts (EN+VI)        (-8 LOC dead keys)
```

## Files archived

```
mockup-screens/               → docs/archive/mockups_2026-06-04/mockup-screens/
mockup-screens-c/             → docs/archive/mockups_2026-06-04/mockup-screens-c/
design/ui-prototype.html      → docs/archive/mockups_2026-06-04/design/ui-prototype.html
```

(`prototype.html` and `render-flow.html` at the repo root were kept; CLAUDE.md
references them as design sketch references.)

## Audit correction registered

The pre-Sprint-1 audit (delivered as 15-phase report on 2026-06-04) made three
claims that turned out to be incorrect, recorded here for the ledger:

1. **`test_gemini_provider.py`, `test_claude_provider.py` "missing"** — actually
   present in `backend/tests/`. The audit recommended creating them; instead
   Sprint 1 fixed the pre-existing template bug that was breaking them.
2. **`services/platform_downloader/` "dead duplicate"** — actually still
   imported by `features/downloader/router.py:27-28`. Kept.
3. **`ai-team-framework/`, `workflows/`, `rules/` "unknown top-level dirs"** —
   `workflows/` and `rules/` are Claude Code Agent OS framework files
   (canonical reference, "Do not modify for project-specific concerns");
   only `ai-team-framework` (a broken gitlink with no `.gitmodules` entry)
   was removed.

## Next

1. Sprint 1.4 — Temp File Audit (read-only, 3-4 days; can run anytime, blocks
   Sprint 6 perf optimisation).
2. Sprint 2 — RenderPlan skeleton dataclass + DB additive column (planned in
   `SPRINT_PLAN_2026-06-04.md`). Requires user approval per sprint.

## Risk register update

| Risk | Status |
|---|---|
| Pydantic deserialize fail on legacy `youtube_url` | Mitigated by `extra="ignore"` on `PrepareSourceRequest` + `QuickProcessRequest`. Gate test `test_legacy_youtube_url_field_is_ignored` pins this contract. |
| `DOWNLOADING` stage name no longer matches behaviour | Still frozen per Sacred Contract #4. Docstring guidance added in `docs/RENDER_PIPELINE.md` will be revisited if a future sprint touches stage names. |
| Drift between docs and code | Reconciled this sprint for the three core docs (`PROJECT_MAP.md`, `ARCHITECTURE.md`, `RENDER_PIPELINE.md`). Other docs under `docs/` may still have legacy references; out of Sprint 1 scope. |

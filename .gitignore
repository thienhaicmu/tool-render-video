# ================================================
# OS / Editor
# ================================================
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo

# ================================================
# Claude Code internal settings
# ================================================
.claude/
.mcp.json

# ================================================
# Python — virtual env & compiled files
# ================================================
backend/.venv/
.venv/
venv/
env/
**/__pycache__/
**/*.pyc
**/*.pyo
**/*.pyd
**/*.pdb
**/.pytest_cache/
**/.mypy_cache/
**/.ruff_cache/

# ================================================
# Node / Electron — dependencies & build outputs
# ================================================
**/node_modules/
desktop-shell/dist/
desktop-shell/dist*
desktop-shell/build/
desktop-shell/out/
desktop-shell/.electron-builder/
desktop-shell/.cache/
desktop-shell/backend-bin/
desktop-shell/*
dist
build
# ================================================
# Runtime data — generated at runtime, NOT source
# ================================================
data/
channels/*/upload/
channels/*/logs/
channels/*/browser-profile/
channels/*/account/

# ================================================
# PyInstaller build outputs
# ================================================
backend/build/
backend/dist/
*.spec

# ================================================
# DB / Logs / Secrets
# ================================================
*.log
*.db
*.sqlite3
.env
.env.*
!.env.example
reports/
temp/

# ================================================
# Media files (VERY IMPORTANT)
# ================================================
*.mp4
*.mov
*.avi
*.mkv
*.webm
*.flv
*.wav
*.mp3
*.png
# Exception: bundled royalty-free BGM library ships WITH the repo so users don't
# re-download (see assets/bgm/ATTRIBUTION.txt for CC-BY credits).
!assets/bgm/
!assets/bgm/**
# ================================================
# AI / ML models & cache
# ================================================
models/
!backend/app/models/
checkpoints/
*.pt
*.pth
*.onnx
*.bin
.cache/

frontend/*.tsbuildinfo
# pytest session sandbox (isolated data dir — never the real data/)
backend/tests/.pytest-data/

# generated visual review artifacts (rebuild via backend/scripts/build_*_library.py / contact sheets)
artifacts/
